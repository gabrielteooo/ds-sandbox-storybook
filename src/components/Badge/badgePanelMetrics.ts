/**
 * Figma Badge 399:66 / 399:67 / 507:38121 — layout metrics (px).
 */
export const DS_BADGE_PANEL = {
  /** Basic dot — Figma `components/badge/component/dotSize` */
  basicDotSizePx: 8,
  /** Status dot — Figma Badge / Status */
  statusDotSizePx: 6,
  /** Base count indicator height — Figma `indicatorHeight` */
  indicatorHeightPx: 20,
  /** Small count indicator height — Figma `indicatorHeightSM` */
  indicatorHeightSmPx: 16,
  countPaddingHorizontalPx: 4,
  countBorderRadiusBasePx: 16,
  countBorderRadiusSmPx: 12,
  statusTextGapPx: 8,
  ribbonPaddingHorizontalPx: 8,
  ribbonRadiusPx: 4,
  ribbonArrowHeightPx: 6,
  ribbonArrowWidthPx: 8,
  basicStoryGapPx: 24,
  statusStoryGapPx: 8,
  ribbonStoryGapPx: 24,
} as const;
