/** Layout metrics for app shell & page templates (Figma library patterns). */
export const DS_APP_SHELL = {
  headerHeightPx: 64,
  sidebarWidthPx: 240,
  contentPaddingPx: 24,
  pageHeaderGapPx: 8,
  pageMaxWidthPx: 1200,
} as const;
